# SPAN Cache

Place attributable NSE Clearing SPAN files here after acquisition. Keep exact source filenames unchanged.

Raw files should only be committed when redistribution is permitted. Otherwise store the checksum/source manifest and a compact derived NIFTY-only cache.

Expected example: nsccl.YYYYMMDD.s.spn
